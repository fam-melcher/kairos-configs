# kairos-configs

Declarative Kairos node configuration for the homelab.

This repository manages the cloud-config based provisioning of all Kairos
nodes: shared base configuration, role definitions (control plane, worker)
and per-node overrides.

See `docs/architecture.md` for the overall design and `docs/adr/` for
architecture decision records.
